/** @format */

import {
  moderateScale,
  horizontalScale,
  screenHeight,
  screenWidth,
  verticalScale,
} from "./responsiveMetrics";
import { globalStyles } from "./globalStyles";
import { FONT_FAMILIES, FONT_SIZE } from "./fonts";
import { COLORS } from "./colors";
import { getFirstTwoLetters } from "./getFirstTwoLetters";

export {
  moderateScale,
  horizontalScale,
  screenHeight,
  screenWidth,
  verticalScale,
  globalStyles,
  COLORS,
  FONT_FAMILIES,
  FONT_SIZE,
  getFirstTwoLetters,
};
